# 依赖（本项目零**运行时**依赖；node_modules 只在本地与 CI 构建时存在）
# ⚠ package-lock.json 必须提交：构建期依赖 @resvg/resvg-js（OG 分享图）要靠它锁版本，
#   不提交的话 CI 每次装到的可能是不同版本 —— 而且 npm ci 根本没有 lock 可用。
node_modules/
pnpm-lock.yaml

# 构建产物：dist 由 npm run build 生成
dist/

# 小程序首屏快照：由 scripts/build.mjs / build-snapshot.mjs 从 data/ 生成，**不入库**（D-027）。
# 它**每次构建都不同**，有两个各自独立的原因 —— 两个都得治才能稳，所以别指望「改成数据时刻」：
#   · 内嵌构建时刻：generatedAt 与由 now 派生的预测值、信号时间窗
#   · 不确定性区间：1000 次 bootstrap，默认 Math.random（src/lib/predict.mjs 的 bootstrapCI）
# 纳入版本管理只会得到一个长期 modified 的文件 —— 那等于训练人忽略 git status。
# 但它**必须存在于磁盘上**：miniprogram/utils/api.js 在模块顶层 import 它，所以测试前置
# （package.json 的 pretest）与微信开发者工具都要先跑 scripts/build-snapshot.mjs。
miniprogram/data/snapshot.js

# 运行期数据：保留（这是产品本体），只忽略临时文件
data/*.tmp-*

# ⚠ F9 订阅名单含 openid，属于用户个人信息，**绝不能进仓库**。
#   它和其余 data/ 文件放在同一个目录，而 CI 会 `git add data` ——
#   不显式排除就会被推到公开仓库里。
data/subscriptions.json

# ⚠ TLS 私钥与证书，**绝不能进仓库**。
#   这份是腾讯云（TrustAsia DV）签发的 reset.example.com 证书包，
#   里面 `reset.example.com.key` 是私钥 —— 一旦推到公开仓库，
#   等于把 reset.example.com 的 TLS 身份公开送人（可被用于中间人）。
#   git 历史是永久的：事后删文件也删不掉已 push 的那份，只能吊销重签。
#   它同时被 .dockerignore 排除（Dockerfile 是 `COPY . .`，不排除会进镜像层）。
#
#   按腾讯云证书包的**解压命名约定**（`<域名>_nginx/`）写，不写死具体域名 ——
#   证书 90 天到期、回来重签时目录名可能变（或换域名），写死了就会静默失效。
*_nginx/
*.csr

# 微信开发者工具的本地私有配置（含个人设置、需要忽略）
project.private.config.json

# 开发者工具以 miniprogram/ 为根打开时，会在子目录再生成一份 —— 内含真实 appid，
# 而仓库根那份有意用 touristappid 占位，所以这份不进仓库。
# ⚠ 必须锚定路径：裸写 project.config.json 会把要跟踪的根配置一起忽略掉。
/miniprogram/project.config.json

# 系统 / 编辑器
.DS_Store
*.swp
.idea/
.vscode/
