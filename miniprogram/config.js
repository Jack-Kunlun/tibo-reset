/**
 * 小程序运行时配置。
 *
 * ⚠ 上线前必须做两件事，否则所有 request 都会走 fail 回调（静默失败，不弹错）：
 *
 *   1. 在小程序后台「开发管理 → 开发设置 → 服务器域名 → request 合法域名」
 *      里加上 apiBase 的域名。**该域名必须已完成 ICP 备案** ——
 *      这是微信侧的硬要求，境外域名备不了案。
 *   2. 确认 apiBase 走 https。
 *
 * 在域名配好之前，把 enabled 保持 false：小程序会退回内置快照（data/snapshot.js），
 * 首屏照常完整显示，只是数据停留在构建时刻。**域名没配不会白屏**，这是刻意的设计。
 */
export default {
  /** 后端地址，不带结尾斜杠 */
  apiBase: 'https://tibo-reset.example.com',

  /** 是否允许联网。false 时只用内置快照，不做任何网络请求 */
  enabled: false,

  /** 单次请求超时（毫秒）。跨境链路，给宽一点 */
  timeoutMs: 8000,

  /** 前台驻留时的自动刷新间隔（毫秒） */
  refreshIntervalMs: 60000,

  /**
   * F9 订阅消息模板 ID。
   *
   * 空字符串 = 功能整体关闭（页面上不显示提醒入口）。
   * 到微信后台「订阅消息」建好模板后把模板 ID 填在这里。
   *
   * ⚠ 只能做「一次授权 = 一次通知」：长期订阅不对工具类目开放，
   *   推完一条额度就没了，要再收到得重新进小程序点一次。
   *   这是平台限制，不是这里没实现。
   */
  subscribeTemplateId: '',

  /** 点通知后跳转的小程序页面 */
  subscribePage: 'pages/index/index',
};
